set default=0
set timeout=8
set timeout_style=menu
set pager=0
set gfxmode=1024x768,auto
set gfxpayload=keep
insmod all_video
insmod png
insmod font
if loadfont "${prefix}/fonts/unicode.pf2"; then
  insmod gfxterm
  if terminal_output gfxterm; then
    if [ -f "${prefix}/themes/imagine/theme.txt" ]; then
      set theme="${prefix}/themes/imagine/theme.txt"
      export theme
    fi
  fi
fi

menuentry "Try or install ImagineOS" --class imagineos {
  linux /live/vmlinuz boot=live components quiet splash username=imagine hostname=imagineos
  initrd /live/initrd.img
}

menuentry "ImagineOS (safe graphics)" --class imagineos {
  linux /live/vmlinuz boot=live components quiet splash nomodeset username=imagine hostname=imagineos
  initrd /live/initrd.img
}

menuentry "Restart" {
  reboot
}

menuentry "Power off" {
  halt
}
